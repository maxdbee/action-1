name: My 01-f Workflow

on: push

jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
        - name: Welcome message
          run: echo "My first job on GA"

        - name: List files
          run: ls

        - name: Read file
          run: cat README.md
